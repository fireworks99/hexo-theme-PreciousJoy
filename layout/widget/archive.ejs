<% if (site.posts.length){ %>
  <div class="widget-box">
    <h3 class="widget-title-archive"><%= __('archives') %></h3>
    <div class="widget">
      <%- list_archives({show_count: theme.show_count, type: theme.archive_type}) %>
    </div>
  </div>
<% } %>